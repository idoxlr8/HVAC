<?php

/**
 * This is a dummy action that does nothing but should have no permissions
 * limitations.  It is used as the default action when running the email cron
 * job.
 */
class actions_email_cron {
	function handle(&$params){
	
	}
}